spring:
  application:
    name: gestion-inventario

  datasource:
    url: jdbc:mysql://mysqlcibertec.mysql.database.azure.com:3306/inventario
    username: administrador
    password: '@i202316885'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: false      
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect 

server:
  port: 8080
